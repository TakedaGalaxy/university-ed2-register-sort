#include "main.hpp"

int main(int argc, char **argv)
{
  /*
  printf("sizeof(ITEM_VENDA) = %ld\n", sizeof(ITEM_VENDA));
  gerar_array_iv("teste.dat", 512000, 42);
  */

 auto f = fopen("", );
 

  return 0;
}